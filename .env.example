# 服务器配置 - 局域网访问
PORT=8082
HOST=0.0.0.0
NODE_ENV=development

# 数据库配置 - 使用完整的连接字符串
# 格式: <db_type>://<username>:<password>@<host>:<port>/<database>?sslmode=<ssl_mode>
# 示例: postgres://lgr:lgr@127.0.0.1:5432/lgr?sslmode=allow
DB_STRING=postgres://lgr:lgr@127.0.0.1:5432/lgr?sslmode=allow
# 可选：单独指定数据库类型（用于驱动匹配）
DB_TYPE=postgres

# 认证配置
API_KEY=comic
JWT_SECRET=d2015833994569e3a4e3

# CORS配置 - 允许所有来源访问
CORS_ORIGIN=*

# 日志配置
LOG_LEVEL=INFO
LOG_FORMAT=simple
LOG_FILE=

# 注意：以下配置已迁移到数据库，可在系统设置页面中修改：
# - 存储路径：ARCHIVE_PATH, THUMBNAIL_PATH, CACHE_PATH, PLUGIN_PATH
# - 扫描设置：ENABLE_AUTO_SCAN, ENABLE_INITIAL_SCAN, SCAN_INTERVAL_CRON
# - 缩略图设置：ENABLE_AUTO_THUMBNAIL, THUMBNAIL_INTERVAL_CRON
# - 性能设置：MAX_CONCURRENT_JOBS, SCAN_FALLBACK_INTERVAL_MS, BSDTAR_PATH, PERL_PATH
# - 数据库检查：ENABLE_DB_CHECK, DB_CHECK_INTERVAL_CRON, ENABLE_INITIAL_DB_CHECK
#
# 注意：以下配置保留在.env中（初始化顺序问题）：
# - 日志设置：LOG_LEVEL, LOG_FORMAT, LOG_FILE

# 迁移控制（可选）
# DISABLE_AUTO_MIGRATION=true